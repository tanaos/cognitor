// Monorepo root — not compiled directly.
// Each app has its own tsconfig.json.
{
    "compilerOptions": {
        "strict": true,
        "skipLibCheck": true
    },
    "exclude": ["node_modules", "apps", "packages"]
}
